server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/msaad809/configserverlabfinaltask
          default-label: main

  security:
    user:
      name: configuser
      password: configpass
